import { AppOrderByPipe } from './app-order-by.pipe';

describe('AppOrderByPipe', () => {
  it('create an instance', () => {
    const pipe = new AppOrderByPipe();
    expect(pipe).toBeTruthy();
  });
});
